# hdt2_deep

## DCGAN_Pokemon.ipynb

La carpeta `pokemon_sprites` (dataset de imágenes) está en `.gitignore` y no se sube al
repo. Si es la primera vez que corres el notebook en tu máquina, en la celda que sigue a
la construcción del dataset debes quitar el comentario de la línea:

```python
download_pokemon_sprites()
```

para descargar las 898 imágenes de sprites de Pokémon antes de continuar con el resto
del notebook.